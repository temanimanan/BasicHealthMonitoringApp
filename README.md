# BasicHealthMonitoringApp

Application Requirements

Develop an UI similar to the Matlab UI shown in the Figure for android phones. Use the emulator to display the UI. For graphing use the graphView class provided in the course webpage. Take a sample array of floats. Whenever the Run button is hit the array is plotted in the canvass. On clicking the stop button the view is cleared.